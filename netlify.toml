[Settings]
ID = "c1d2225a-094b-4d3b-8709-8dad16ea17fe" # Settings in the [build] context are global and are applied to all contexts unless otherwise overridden by more specific contexts.  

[build]
  # This is the directory to change to before starting a build.
  base  = "portfolio/" # NOTE: This is where we will look for package.json/.nvmrc/etc, not root.
  # This is the directory that you are publishing from (relative to root of your repo)
  publish = "portfolio/build/"
  # This will be your default build command
  command = "yarn build"